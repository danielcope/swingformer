# Feature docs

One file per feature: `<slug>.md`, intent → design → plan, each approved
before the next (the `feature` skill has the template). Kept after shipping
as the record of what was asked and decided; `status: done` or `dropped`.
